import { defineConfig } from 'vite';import laravel from 'laravel-vite-plugin';export default defineConfig({  plugins: [    laravel({      input: [        'resources/css/app.css', //  CSS-        'resources/js/app.js'   //  JS-      ],      refresh: true,    }),  ],  server: {    hmr: {      host: 'localhost',    },    watch: {      usePolling: true,    },  },});
